rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // All application access uses Firebase Admin SDK on the server.
    // Direct client SDK access is denied for every collection.
    match /{document=**} {
      allow read, write: if false;
    }
  }
}
